# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
rust-version = "1.85"
name = "rusty_aac"
version = "0.5.0"
authors = ["Mata Network <https://www.mata.network>"]
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "Pure-Rust AAC-LC decoder + encoder, no C/FFI. ~5x faster than FFmpeg's native AAC encoder and beats it on mono music at 128k+ (PEAQ-measured). Level-invariant transient detection, joint stereo rate loop, ADTS/MP4/LATM-LOAS, 5.1 layouts, HE-AAC signalling."
homepage = "https://github.com/Remade-With-Rust/remade_ffmpeg_rs"
readme = "README.md"
keywords = [
    "aac",
    "audio",
    "codec",
    "decoder",
    "encoder",
]
categories = [
    "multimedia::audio",
    "multimedia::encoding",
]
license = "Apache-2.0"
repository = "https://github.com/Remade-With-Rust/remade_ffmpeg_rs"

[features]
default = ["simd"]
lab = []
simd = []
simd-avx512 = ["simd"]

[lib]
name = "rusty_aac"
path = "src/lib.rs"

[[example]]
name = "aacenc"
path = "examples/aacenc.rs"
required-features = ["lab"]

[[example]]
name = "aacexport"
path = "examples/aacexport.rs"
required-features = ["lab"]

[[example]]
name = "aacharvest"
path = "examples/aacharvest.rs"
required-features = ["lab"]

[[example]]
name = "aacquality"
path = "examples/aacquality.rs"
required-features = ["lab"]

[dependencies]
rustfft = "6.4.1"

[dev-dependencies.rusty_alloc-api]
version = "0.3"

# TenzorPipe: standalone workspace so `cargo test --manifest-path vendor/rusty_aac/Cargo.toml`
# uses this crate's lockfile instead of joining the parent workspace.
[workspace]
